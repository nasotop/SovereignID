/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CareerSummary } from '../../models/career-summary';
import { UpdateCareerRequest } from '../../models/update-career-request';

export interface AcademyInstitutionsInstitutionIdCareersCareerIdPatch$Params {
  institutionId: string;
  careerId: string;
      body: UpdateCareerRequest
}

export function academyInstitutionsInstitutionIdCareersCareerIdPatch(http: HttpClient, rootUrl: string, params: AcademyInstitutionsInstitutionIdCareersCareerIdPatch$Params, context?: HttpContext): Observable<StrictHttpResponse<CareerSummary>> {
  const rb = new RequestBuilder(rootUrl, academyInstitutionsInstitutionIdCareersCareerIdPatch.PATH, 'patch');
  if (params) {
    rb.path('institutionId', params.institutionId, {});
    rb.path('careerId', params.careerId, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CareerSummary>;
    })
  );
}

academyInstitutionsInstitutionIdCareersCareerIdPatch.PATH = '/academy/institutions/{institutionId}/careers/{careerId}';
